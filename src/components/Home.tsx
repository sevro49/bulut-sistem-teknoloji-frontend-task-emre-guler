import ProductList from '@/components/ProductList';

const Home = () => {
  
  return (
    <div className='w-full h-screen flex items-center justify-center px-[1rem] sm:px-[5rem] lg:px-[8rem] xl:px-[10rem] 2xl:px-[25rem]'>
      <ProductList />
    </div>
  );
};

export default Home;
